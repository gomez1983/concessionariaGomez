services:
  mysql:
    image: mysql:8.0
    container_name: concessionaria-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: catita01
      MYSQL_DATABASE: concessionaria
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
